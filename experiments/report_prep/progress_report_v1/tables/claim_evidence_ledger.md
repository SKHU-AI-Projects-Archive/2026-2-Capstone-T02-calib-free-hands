# Claim ledger: what we may write, and how

One row per claim the progress report is allowed to make. If a sentence in the report is not covered by a row here, it is not yet supported and should not be written.

| claim id | claim we may write | supporting experiment | supporting numbers | evidence level | allowed wording | wording to avoid | scope | external confirmation needed | report section |
|---|---|---|---|---|---|---|---|---|---|
| C01 | Holding all cached hand predictions fixed, replacing the pipeline's focal convention with the dataset-provided reference focal reduced the median root error from 2881.885 mm to 78.54 mm. | CAM-EXP-002 | 5000 px vs 922.8 px; median root error 2881.885 mm vs 78.54 mm (~36.69x lower); root-aligned MPJPE identical at 34.404 mm | ROBUST_BUT_SINGLE_DATASET | 동일한 hand prediction을 고정한 채 파이프라인의 focal convention을 dataset-provided reference focal로 교체했을 때, median root error가 2881.885 mm에서 78.54 mm로 감소했다 (약 36.69배). | The pipeline is broken / all monocular hand pipelines have metre-scale error / nearly two orders of magnitude / the camera, not the hand model, is the problem. | one dataset, one working-distance regime. This isolates the focal term on fixed predictions; substantial absolute error remains under the reference focal condition, so it does not attribute the whole absolute error to the camera. | yes, for the magnitude | 5 (Experiment 2) |
| C02 | Focal error propagates almost proportionally into absolute hand depth, while leaving the hand's own shape untouched. | CAM-EXP-002 | 5 % -> 32.801 mm, 10 % -> 65.602 mm, 20 % -> 131.204 mm; root-aligned MPJPE unchanged at 34.404 mm | ROBUST_BUT_SINGLE_DATASET | focal-length sensitivity of absolute hand depth; the near-linear relation is expected from the pipeline equation Z proportional to f. | full camera calibration sensitivity; we discovered that depth scales with focal length. | FOCAL ONLY. Principal point, anisotropic focal and distortion were not tested downstream. | no for the direction, yes for the mm | 5 (Experiment 2) |
| C03 | Reconstructing 3D from the released multi-view 2D agrees with the provided 3D to about 4 mm, while a wrong-hand control is ~48x worse. | CAM-EXP-001.3 | same-hand median 3.9977 mm, p90 6.4816 mm; other-hand 192.3047 mm | CONFIRMED_IN_CURRENT_ENVIRONMENT | released 2D 관측으로 재구성한 3D와 제공된 3D는 약 4 mm 수준의 자기일관성(self-consistency)을 보였다. | GigaHands 3D는 실제 손 위치에서 4 mm 정확하다 / we validated GigaHands against ground truth. | internal self-consistency of one dataset's own annotations | not applicable - this is a consistency claim, not an accuracy claim | 4 (Experiment 1) |
| C04 | Published single-frame calibration methods do not reach the +/-5 % focal target on this data. | CAM-EXP-003 | median focal error 16.3102 / 24.9645 / 20.8152 / 23.5346 %, 175 views | ROBUST_BUT_SINGLE_DATASET | 본 평가 조건에서 단일 프레임 방법만으로는 목표 정확도에 도달하지 못했다. | AnyCalib/GeoCalib are inaccurate methods; these models fail. | GigaHands optics, 1280x720, raw images, pinhole camera model | yes | 6 (Experiment 3) |
| C05 | Accounting for lens distortion roughly halves the single-frame focal error, but does not close the gap. | CAM-EXP-003.1, re-analysed in CAM-EXP-004.1 | AnyCalib 16.3102 -> 9.546 %; paired gain 6.537 pp [4.573, 8.491] with views as the resampling unit | ROBUST_BUT_SINGLE_DATASET | distortion-aware 모델이 pinhole 모델보다 일관되게 우수했고, view/카메라/시퀀스 어느 군집 수준에서도 신뢰구간이 0을 포함하지 않았다. | distortion explains the error; quoting the frame-level CI as if 1400 frames were independent. | \|k1\| 0.34-0.43 only, so no dose-response conclusion | yes | 6 (Experiment 3) |
| C06 | The residual focal error of the best model is dominated by a stable between-view component - a per-(sequence, camera) static-view bias - rather than by frame-to-frame noise. | CAM-EXP-004 | 98.08 % of AnyCalib's squared log focal error is the between-view component; median \|view bias\| 9.63 % vs within-view sd 1.43 % | ROBUST_BUT_SINGLE_DATASET | AnyCalib 잔여 오차는 동일 고정 관점 내 프레임 노이즈보다 (sequence, camera) 관점별 안정적 편향에 의해 지배되었다. 분해 단위는 고정 관점이며, physical camera 단위의 편향을 측정한 것이 아니다. | 98.1 %의 오차가 physical-camera bias 때문이다 / 모든 고정카메라 calibration 모델의 오류는 view bias 때문이다. | one rig; the decomposition unit is the (sequence, camera) static view, NOT the physical camera. Physical camera is a resampling unit used for robustness in CAM-EXP-004.1. | yes | 7 (Experiment 4) |
| C07 | Within this setting, aggregating up to 64 frames of a static camera does not improve focal accuracy. | CAM-EXP-004 + CAM-EXP-004.1 | AnyCalib 9.89 -> 9.966 %; E2 6.46 -> 6.74 %; every paired step CI contains zero | ROBUST_BUT_SINGLE_DATASET | within the evaluated GigaHands static-camera condition, up to 64 frames, 프레임 수를 늘리는 것은 초점거리 정확도를 개선하지 못했다. | multi-frame aggregation never helps; 8 frames are always enough. | N <= 64, GigaHands static cameras | yes | 7 (Experiment 4) |
| C08 | Combining two calibration model families with opposite signed bias is the most promising direction found so far. | CAM-EXP-004, audited in CAM-EXP-004.1 | AnyCalib 9.89 % -> E2 6.138 % (6.46 % in a separate execution on the same 175 benchmark views, 5.73-8.797 % across retrospective leave-one-sequence-out folds); gain +2.23 pp, CI excludes zero at all cluster levels | EXPLORATORY_INTERNAL_VALIDATION | frozen exploratory ensemble / 현재까지 확인된 가장 유망한 baseline / strongest baseline identified so far. | our final method; our proposed method; E2 achieves 6.14 %. | selected as the best of five candidates on the same 175 views it is reported on | YES - this is the number the sealed holdout exists for | 7 and 8 |
| C09 | AnyCam's official focal-selection mechanism is not identifiable under a static camera. | CAM-EXP-004 (source), CAM-EXP-004.1 (numeric) | spread across its own 32 focal candidates 2.73e-09 static vs 0.223 moving | CONFIRMED_IN_CURRENT_ENVIRONMENT | AnyCam의 공식 focal-selection mechanism은 camera-induced motion을 이용하므로, 본 연구의 static-camera condition에서는 focal candidates가 식별되지 않았다. | AnyCam performed poorly; AnyCam is a bad method; we benchmarked AnyCam. | method-assumption mismatch with our deployment condition | no; but the end-to-end behavioural test is still missing (Windows blocker) | 6 (small subsection on external video methods) |
| C10 | GeoCalib's output varies between runs on the same image, but the aggregate conclusions are unchanged. | CAM-EXP-004.1 | median spread 1.18 %, p90 6.85 %, max 41.5 %; aggregate median shift ~0.32 pp; AnyCalib 100.0 % bit-identical | OPEN_ISSUE | GeoCalib은 동일 입력에서도 실행 간 변동이 관찰되었으나, 전체 중앙값 변화는 약 0.32 %p로 주요 결론을 변경하지 않았다. | omitting it; or presenting any single GeoCalib number without the tolerance. | this environment and this GeoCalib commit | no - it needs a fix attempt, not confirmation | 9 (Limitations), details in the appendix |
| C11 | Different experiments use different subsets because a camera experiment and a hand experiment need different things to be valid. | CAM-EXP-001.3 QC + CAM-EXP-003 camera manifest | camera-clean 175 views; bimanual-clean 154 views / 16,413 frames | CONFIRMED_IN_CURRENT_ENVIRONMENT | camera-clean / hand-clean / bimanual-clean subsets are different by design. | implying the datasets shrank because of failures, or that hand QC removed camera views. | GigaHands demo | no | 3 (Datasets and evaluation environment) |
| C12 | Our statistical unit is the static camera view, not the frame. | CAM-EXP-004.1 | the same point estimate with CIs ~2.5x wider under view clustering; e.g. +6.54 pp [+5.75, +7.30] frame-level vs [+4.57, +8.49] view-level | CONFIRMED_IN_CURRENT_ENVIRONMENT | 175 fixed-camera views, 8 frames per view, 1400 frames in total; statistical unit = view. | n = 1400; 1400 independent samples. | all camera experiments | no | 3 and every results section |
